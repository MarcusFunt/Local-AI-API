[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: end-to-end test that needs a running audio-enabled gateway; skips when one is not reachable
