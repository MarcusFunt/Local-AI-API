### response

Use this only for a final user-facing answer.

The only valid arguments are `{"text":"your answer"}`. Do not use a key
named `message`, `response`, or `content`.

Example: `{"tool_name":"response","tool_args":{"text":"Done."}}`
