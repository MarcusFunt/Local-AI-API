## Problem solving

Use a listed tool immediately when work is needed. For a question that needs no
tool, call `response`. Inspect tool output before the next action, and use
`response` only when the task is complete or blocked.
